{%- liquid
  assign range_products = section.blocks | where: "type", "product"
-%}

<section class="sec" id="range" data-scene="3">
  <div class="wrap">
    <div class="glass range rv">
      <div class="range-head">
        <div>
          <span class="kicker">{{ section.settings.kicker | default: "The full range" }}</span>
          <h2 class="d2" style="margin-top:10px">{{ section.settings.heading | default: "Every room, one shelf" }}</h2>
        </div>
        <p class="body-s" style="max-width:34ch;margin-bottom:4px">{{ section.settings.subheading | default: "Floors, taps, kitchen, laundry, bathroom and hands. Fourteen plant-based formulas that replace every harsh bottle under your sink." }}</p>
      </div>
      <div class="stripwrap">
        <div class="rangerow">
          {%- if range_products.size > 0 -%}
            {%- for block in range_products -%}
              {%- liquid
                assign prod_img = block.settings.image
                if prod_img == blank and block.settings.product != blank
                  assign prod_img = block.settings.product.featured_image
                endif
              -%}
              {%- if prod_img != blank -%}
                <img src="{{ prod_img | image_url: width: 250 }}" class="pimg" alt="{{ block.settings.title | default: prod_img.alt | escape }}" width="{{ prod_img.width }}" height="{{ prod_img.height }}" style="width: auto; object-fit: contain;">
              {%- else -%}
                <span class="pimg {{ block.settings.fallback_class | default: 'p-kitchen' }}" role="img" aria-label="{{ block.settings.title | escape }}"></span>
              {%- endif -%}
            {%- endfor -%}
          {%- else -%}
            <!-- Default Fallbacks from the prototype HTML -->
            <span class="pimg p-floor" role="img" aria-label="Purelane floor cleaner"></span>
            <span class="pimg p-tap" role="img" aria-label="Purelane tap cleaner"></span>
            <span class="pimg p-kitchen" role="img" aria-label="Purelane kitchen cleaner"></span>
            <span class="pimg p-toilet" role="img" aria-label="Purelane toilet cleaner"></span>
            <span class="pimg p-laundry" role="img" aria-label="Purelane laundry detergent"></span>
            <span class="pimg p-dish" role="img" aria-label="Purelane dishwash gel"></span>
            <span class="pimg p-metal" role="img" aria-label="Purelane copper, bronze and brass cleaner"></span>
            <span class="pimg p-wm" role="img" aria-label="Purelane washing machine cleaner"></span>
            <span class="pimg p-handwash" role="img" aria-label="Purelane hand wash"></span>
            <span class="pimg p-eraser" role="img" aria-label="Purelane magic eraser"></span>
          {%- endif -%}
        </div>
      </div>
      <p class="striphint">{{ section.settings.hint_text | default: "Swipe to see the full shelf" }}</p>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Purelane Full Range",
  "tag": "section",
  "class": "section-purelane-range",
  "settings": [
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker",
      "default": "The full range"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Every room, one shelf"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Floors, taps, kitchen, laundry, bathroom and hands. Fourteen plant-based formulas that replace every harsh bottle under your sink."
    },
    {
      "type": "text",
      "id": "hint_text",
      "label": "Hint Text",
      "default": "Swipe to see the full shelf"
    }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Shelf Product",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product Reference"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Custom Product Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Accessibility Label",
          "default": "Purelane product"
        },
        {
          "type": "text",
          "id": "fallback_class",
          "label": "CSS Fallback Class",
          "default": "p-kitchen",
          "info": "Values: p-floor, p-tap, p-kitchen, p-toilet, p-laundry, p-dish, p-metal, p-wm, p-handwash, p-eraser"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Purelane Full Range"
    }
  ]
}
{% endschema %}
